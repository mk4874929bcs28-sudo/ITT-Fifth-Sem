#include <stdio.h>

int main() {
    char ch;
    
    while (scanf("%c", &ch) == 1) {
        if (ch == ' ') {
            printf("\n");
        } else {
            printf("%c", ch);
        }
    }
    
    return 0;
}
